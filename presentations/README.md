# Presentations

All SNH talk material lives here — decks, speaker notes, and prep.

- [`pnt-workshop/`](pnt-workshop/) — the Personal Network Toolkit workshop talk: a
  [Slidev](https://sli.dev/) deck (`slides.md`) plus the long-form script
  (`presentation_long.txt`) and voice notes (`talk_voice_notes.txt`). Imported July 2026 from the
  standalone `richbodo/pnt-workshop` repo.
- [`dwebcamp-berlin-2026/`](dwebcamp-berlin-2026/) — DWeb Camp Berlin talk (week of 2026-07-06),
  in prep.

Run a Slidev deck locally from the repo root with `just slides <folder>` (defaults to
`pnt-workshop`) — it installs deps and opens the slideshow at localhost:3030.
